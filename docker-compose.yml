version: '3.8'
services:
  nlp-service:
    image: nlp-platform:latest
    ports:
      - '5000:5000'
    environment:
      - DATABASE_URL=mongodb://db:27017/nlp
      - PORT=5000

  db:
    image: mongo:latest
    volumes:
      - db-data:/data/db

volumes:
  db-data:
